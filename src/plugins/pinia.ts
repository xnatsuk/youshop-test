// Pinia store is the official state management library for Vue 3.
// It is inspired by Vuex but is designed to be faster, more flexible, and easier to use.

import { createPinia } from 'pinia'

export default createPinia()
